# Requirements registry update note

For the merge of the Pro billing adapter, update MP-045 evidence to: `product package and explicit entitlements implemented; official Stripe 15.4.0 hosted Checkout/Portal and webhook-verification adapter implemented fail-closed; public billing activation remains blocked on authenticated customer identity plus owner-controlled Stripe credentials, verified product/price and test-mode lifecycle evidence.`

This note exists to prevent scope loss while the final registry reconciliation is performed in the closure pass.
